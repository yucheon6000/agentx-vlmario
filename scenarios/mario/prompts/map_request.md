# Map Generation Request
Task: Generate map {map_num}/{total}
## Instructions
1. Return ONLY the ASCII map in a ```ascii code block
2. Include 'M' (start) and 'F' (exit flag)
3. All rows must be exactly the same length (pad with '-')
4. Make the map playable within 60 seconds

## ASCII Tile Reference
{map_ascii_guide}

## Response Format
```ascii
[Your map here]
```
