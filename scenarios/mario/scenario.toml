[green_agent]
endpoint = "http://127.0.0.1:9100"
# pre_cmd = "bash scenarios/mario/run_generator.sh"  # Disabling as it was empty and causing errors on Windows
cmd = "python scenarios/mario/mario_map_evaluator.py --host 127.0.0.1 --port 9100"

[[participants]]
role = "agent"
endpoint = "http://127.0.0.1:9110"
cmd = "python scenarios/mario/mario_map_designer.py --host 127.0.0.1 --port 9110"
env = { "MODEL" = "gemini-3-flash-preview" }

[config]
num_maps = 25
# Optional: reference map for consistent evaluation (score: 1-20)
# reference_video_path = "./reference_gameplay.mp4"
# reference_score = 12.0
jar_output_dir = "./"
jar_output_name_template = "{role}_gameplay_{ts}_{map_idx}.mp4"
